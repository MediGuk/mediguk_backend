```mermaid
sequenceDiagram
    autonumber
    participant C as Cliente (Frontend)
    participant API as AuthController
    participant S as AuthService
    participant SS as SessionService
    participant R as Repositories
    participant JWT as JwtService

    Note over C, JWT: POST /auth/refresh (Cookie: refreshToken & fingerprint)

    C->>API: Intento de renovación automática
    API->>S: refresh(oldRefreshToken, fingerprintRaw)
    
    S->>SS: validateRefreshToken(oldRefreshToken)
    SS->>R: findByRefreshTokenAndRevokedFalse()
    R-->>S: Session Entity

    alt ❌ Sesión caducada (> 30 días) o Fingerprint no coincide
        S->>R: update session.revoked = true
        S-->>C: 401 Unauthorized (Re-login required)
    end

    rect rgb(45, 45, 45)
        Note over S, SS: Proceso de Rotación
        S->>SS: rotateRefreshToken(session)
        Note right of SS: Genera nuevo UUID y pisa el anterior
        SS->>R: save(newRefreshToken)
    end

    S->>JWT: generateToken(userId, sessionToken)
    JWT-->>S: nuevo accessToken (JWT)

    S-->>API: AuthResult { newJwt, newRefreshToken }

    Note over API: 🍪 El navegador pisa el refreshToken viejo<br/>con el nuevo (Path: /auth/refresh)
    API->>C: 200 OK + Set-Cookie (newRefreshToken) + Body (newJwt)