```mermaid
sequenceDiagram
    autonumber
    participant C as Cliente (Frontend)
    participant API as AuthController
    participant S as AuthService
    participant SS as SessionService
    participant R as Repositories
    participant JWT as JwtService

    Note over C, JWT: POST /auth/refresh (Cookie: refreshToken & fingerprint)

    C->>API: Intento de renovación automática
    API->>S: refresh(actualRefreshToken, fingerprintRaw)
    
    S->>SS: getValidSession(actualRefreshToken, fingerprintRaw)
    
    rect rgb(45, 45, 45)
        Note over SS, R: Triple Check de Seguridad
        SS->>R: findByRefreshTokenAndRevokedFalse()
        SS->>SS: Check Expiration
        SS->>SS: passwordEncoder.matches(fingerprint)
    end

    alt ❌ Algún check falla
        SS->>R: update session.revoked = true
        SS-->>C: 401 Unauthorized (Re-login required)
    end

    S->>SS: rotateRefreshToken(session)
    S->>JWT: generateToken(userId, sessionToken)

    S-->>API: AuthResult { newJwt, newRefreshToken, fingerprintRaw }
    API->>C: 200 OK + Body (AuthResult)